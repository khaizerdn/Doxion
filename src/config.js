// src/config.js
export const config = {
    idleTimeout: 999000, // 60 seconds in milliseconds
    showTimer: false, // Default to false, set to true for testing
    fontFamily: 'Arial, sans-serif', // Default font family (matches index.css)
  };